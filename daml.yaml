# for config file options, refer to
# https://docs.daml.com/tools/assistant.html#project-config-file-daml-yaml

sdk-version: 2.6.4
name: daml-it
source: daml
# init-script: Main:setup -- uncomment and select the Module and setup function to initialize the navigator
version: 0.0.1
dependencies:
  - daml-prim
  - daml-stdlib
  - daml-script
